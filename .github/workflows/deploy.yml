name: Deploy to Server
# adding comment to test with a change
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Deployment Script
        run: |
          curl -s "https://yourdomain.com/deploy.php?key=${{ secrets.DEPLOY_KEY }}"
